import { legendsNFTContract } from "@/config/legendary-labs-contracts";

export async function redeemPromoTicket() {
  console.log( "good");

  try {
   
  } catch (error) {
    console.log(error)
  }
}
